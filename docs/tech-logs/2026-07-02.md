# 2026-07-02 技术日志

## 背景

项目仓库位于 `/home/boss/shuati_platform/shuati_platform`，当前只有 `.git`，还没有项目脚手架。Windows 侧的 `C:\AI\shuati_platform` 不是本次实现目标目录，后续正式文件应写入 WSL 仓库。

## 已确认判断

- MVP 目标是个人练手 OJ 平台，但可以支持小规模课堂或公网试用。
- 后端使用 C++ 和 `cpp-httplib`。
- 前端使用原生 HTML/CSS/JavaScript，由 C++ 服务托管静态文件。
- 判题模式只做 ACM 标准输入输出。
- 提交语言只支持 C++。
- 数据库使用 MySQL 8.0。
- 判题队列使用 MySQL 任务表，不只放在内存里。
- 判题沙箱使用 Docker；MVP 允许后端直接调用 `docker run`。
- 部署形态是一台 Linux 机器同时运行后端、MySQL 和 Docker 判题环境。
- UI 风格参考 `https://datacollector.cn/`：白色、克制、工具型、弱装饰。
- 测试策略包括 Google Test、LLM + curl、Python requests、Playwright。

## 可复用约束

- 自研基础设施要控制在 MVP 规模：固定大小连接池、基础日志、简单 yaml 风格配置、会话管理、固定窗口限流。
- 测试用例文件保存在本地磁盘，MySQL 存路径和元信息。
- 提交源码只保留 1 天；结果元数据可以继续保留。
- 普通用户不能看到测试输入、标准输出和实际输出。
- API 响应统一为 `{code, message, data}`。
- 规格确认后，第一轮实现建议从纵向切片开始，而不是先横向铺开所有模块。
- 每次完成一个可交付任务后，要自动执行 Git 检查、本地提交，并推送到 GitHub；最终回复里说明提交哈希、GitHub 推送结果、验证结果和遗留风险。
- GitHub 远程仓库为 `https://github.com/huachen19867/shuati_platform`。2026-07-02 已完成本地 `main` 首次推送，远端 `origin/main` 可追踪。
- `SPEC.md` 末尾维护“方向盘 TODO 清单”，按阶段勾选推进，防止实现时失焦。

## 用过的命令

```bash
wsl -d Ubuntu -- bash -lc "ls -la /home/boss/shuati_platform/shuati_platform"
wsl -d Ubuntu -- bash -lc "git -C /home/boss/shuati_platform/shuati_platform status --short --branch"
wsl -d Ubuntu -- bash -lc "cd /home/boss/shuati_platform/shuati_platform && git config user.name boss && git config user.email boss@localhost"
wsl -d Ubuntu -- bash -lc "cd /home/boss/shuati_platform/shuati_platform && git add SPEC.md docs && git commit -m 'docs: add oj platform spec'"
git -C \\wsl.localhost\Ubuntu\home\boss\shuati_platform\shuati_platform remote -v
git -C \\wsl.localhost\Ubuntu\home\boss\shuati_platform\shuati_platform ls-remote --heads origin
```

## 踩坑记录

- 当前 shell 默认工作目录是 `C:\AI\shuati_platform`，但 Cursor 里真正要开发的是 WSL 路径 `/home/boss/shuati_platform/shuati_platform`。写文件前必须确认目标路径。
- GitHub 克隆下来的仓库是空仓库，且还没有任何 commit。不能假设已有 README、构建脚本或目录结构。
- 参考 UI 站点是 JavaScript 渲染页面，普通抓取只能看到需要 JavaScript 的兜底内容。需要观察视觉风格时，要用浏览器渲染工具。
- 空仓库首次提交时可能没有 Git 身份配置。为避免影响全局环境，本次只设置仓库本地 `user.name` 和 `user.email`。
- 这个仓库在 WSL 文件系统中，Git 操作优先使用 WSL 内的 `git`。Windows Git 直接操作 `\\wsl.localhost\...` 路径时可能出现 LF/CRLF 提示，容易制造无关噪音。
- WSL 内 `git push` 曾遇到 `GnuTLS recv error (-110)`。可用 Windows Git 配合 `-c core.autocrlf=false` 对 WSL 仓库执行 push，避免换行转换并绕过该 TLS 问题。

## 本次产出

- 根规格文档：`/home/boss/shuati_platform/shuati_platform/SPEC.md`。
- 技术日志索引：`/home/boss/shuati_platform/shuati_platform/docs/tech-logs/README.md`。
- 当天技术日志：`/home/boss/shuati_platform/shuati_platform/docs/tech-logs/2026-07-02.md`。

## P0 地基进展

- 创建基础目录和 `.gitignore`，排除 CMake 生成目录、运行日志、判题临时文件和本地配置覆盖。
- 建立 CMake 工程骨架：`shuati_core`、`shuati_server`、`shuati_tests`。
- 引入 `cpp-httplib v0.48.0` 单头文件，来源为官方 GitHub release。
- 新增 `config/app.yaml` 示例配置。
- 新增 Ubuntu 依赖说明和 `scripts/install_ubuntu_deps.sh`。
- 以 gtest 先写配置解析测试，再实现最小 YAML 风格配置读取。
- 当前 WSL 缺少 `g++/build-essential`，CMake 配置阶段会停在 `No CMAKE_CXX_COMPILER could be found`。sudo 需要密码，无法由代理无交互安装；老板执行依赖脚本后即可继续构建验证。

## P0 提交前验证

- `git diff --check` 已通过；提交前发现过文件末尾多余空白行，已清理。
- `.gitignore` 已验证能忽略 `build/`、`logs/*.log`、`data/testcases/*`、`data/submissions/*`、`data/judge_tmp/*` 和 `config/*.local.yaml`。
- `cpp-httplib` 头文件内版本宏为 `CPPHTTPLIB_VERSION "0.48.0"`。
- `cmake -S . -B build -DSHUATI_BUILD_TESTS=ON` 已执行，当前失败原因为本机缺少 `g++/c++`，不是已知 CMake 脚本语法错误。
- `sudo -n true` 返回 `SUDO_NEEDS_PASSWORD`，所以本轮无法自动安装 `build-essential`；安装命令已沉淀到 `scripts/install_ubuntu_deps.sh` 和依赖文档。
- P0 地基主提交为 `883ccb8 chore: build p0 project foundation`，已推送到 GitHub。WSL 内 `git push` 仍会触发 `GnuTLS recv error (-110)`，本轮继续使用 Windows Git 加 `core.autocrlf=false` 推送成功。

## P1 最小可运行服务进展

- 新增 `AppConfig`，从 `config/app.yaml` 读取 `app`、`server` 和 `logs` 的 P1 字段；缺省值保持轻量：`0.0.0.0:8080`、`public/`、`logs/*.log`、日志级别 `info`。
- 新增日志基础设施：`DEBUG`、`INFO`、`WARN`、`ERROR` 四级，大小写不敏感解析，未知级别直接报错；文件日志会自动创建父目录，并按配置级别过滤低级别消息。
- 新增统一 JSON 响应工具，固定输出 `{code,message,data}`，并覆盖字符串转义，避免错误消息里出现引号或换行时破坏 JSON。
- 新增 cpp-httplib 路由装配：`GET /api/health` 返回统一 JSON；静态文件从 `public/` 托管；404 返回统一 JSON；访问日志写入 `logs/access.log`。
- 新增白色克制的最小首页 `public/index.html`，原生 JS 调用 `/api/health` 显示服务状态。
- 新增 gtest 用例：日志级别解析和过滤、JSON 响应和转义、配置默认值和覆盖值、健康检查响应结构。
- 已再次执行 `cmake -S . -B build -DSHUATI_BUILD_TESTS=ON`，仍因缺少 `g++/c++` 停在 `No CMAKE_CXX_COMPILER could be found`；当前不能启动服务做浏览器验收。
